openapi: 3.1.0
info:
  title: OpenAPI Specification
  version: 1.0.0
  description: >-
    A specification generated by
    [openapi-devtools](https://github.com/AndrewWalsh/openapi-devtools).
    Contains 7 endpoints.
paths:
  /login:
    $ref: paths/login.yaml
  /notifications:
    $ref: paths/notifications.yaml
  /transactions/public:
    $ref: paths/transactions/public.yaml
  /users:
    $ref: paths/users.yaml
  /users/search:
    $ref: paths/users/search.yaml
  /transactions:
    $ref: paths/transactions.yaml
  /checkAuth:
    $ref: paths/checkAuth.yaml
components:
  securitySchemes:
    apikey_cookie_connect.sid:
      type: apiKey
      in: cookie
      name: connect.sid
    apikey_header_cookie:
      type: apiKey
      in: header
      name: COOKIE
